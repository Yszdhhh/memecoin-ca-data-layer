# MACRO-DAILY-TRADE-ACTIVITY-MIGRATION-REPAIR-AUDIT-014

| Field | Value |
| --- | --- |
| task_id | `MACRO-DAILY-TRADE-ACTIVITY-MIGRATION-REPAIR-AUDIT-014` |
| auditor role | independent auditor (read-only) |
| audit date | 2026-07-21 (Asia/Shanghai) |
| verdict | **GREEN** |

## Scope

Verify that migration `006_macro_trade_activity_metrics.sql` now targets the
actual unnamed metric-name CHECK generated by migration
`002_macro_daily_metrics.sql`, preserves every existing constraint and metric,
adds only the audited activity section plus two new metrics, and makes no
external operation.

## Check 1 — Unnamed metric_name CHECK default name

Migration 002 (L25-54) creates `macro_daily_chain_metrics`. The metric-name
CHECK at L30-40 is defined inline on the `metric_name` column but references
both `chain` and `metric_name`, making it a complex multi-column expression.
PostgreSQL assigns such unnamed CHECKs the auto-generated name
`{table}_check`. The live run report (013.md L18-19) independently confirms the
observed schema names this constraint **`macro_daily_chain_metrics_check`**.

Migration 006 L9 drops `macro_daily_chain_metrics_check` and L12 re-adds it
with the same name. This matches the actual PostgreSQL default name.

| Evidence | Location |
| --- | --- |
| Unnamed CHECK definition | `002_macro_daily_metrics.sql` L30-40 |
| Live schema observation | `docs/runs/MACRO-DAILY-TRADE-ACTIVITY-LIVE-013.md` L18-19 |
| DROP target | `006_macro_trade_activity_metrics.sql` L9 |
| ADD target | `006_macro_trade_activity_metrics.sql` L12 |

**Result: PASS**

## Check 2 — 006 only drops/re-adds the metric-name CHECK; all other constraints preserved

Migration 006 performs exactly four ALTER TABLE statements:

1. L1-2: DROP `macro_daily_chain_metrics_section_check` (the named section CHECK).
2. L4-6: ADD `macro_daily_chain_metrics_section_check` with `'activity'` added
   to the allowed values (`'capital', 'supply', 'activity', 'timing'`).
3. L8-9: DROP `macro_daily_chain_metrics_check` (the unnamed metric-name CHECK).
4. L11-22: ADD `macro_daily_chain_metrics_check` with expanded metric lists.

**Preserved constraints** (untouched by 006, remain from 002):

| Constraint | Type | 002 Line |
| --- | --- | --- |
| `macro_daily_chain_metrics_chain_check` | CHECK chain IN ('solana','bsc','robinhood') | L28 |
| `macro_daily_chain_metrics_coverage_status_check` | CHECK coverage_status IN (...) | L44 |
| `macro_daily_chain_metrics_completeness_check` | CHECK completeness BETWEEN 0 AND 1 | L50 |
| Robinhood partial_coverage | CHECK (chain <> 'robinhood' OR coverage_status = 'partial_coverage') | L52 |
| UNIQUE (report_day, chain, metric_name, source, query_ref, query_version) | UNIQUE | L53 |

No constraint is removed without being re-added. The section CHECK is widened
(additive only). The metric-name CHECK is widened (additive only).

**Result: PASS**

## Check 3 — Only new additions: activity section, swap_transaction_count, trade_leg_count

**Section CHECK diff** (006 L5-6):

```diff
- CHECK (section IN ('capital', 'supply', 'timing'))
+ CHECK (section IN ('capital', 'supply', 'activity', 'timing'))
```

Only `'activity'` is added.

**Metric-name CHECK diff** (006 L12-22 vs 002 L30-40):

| Chain | Original metrics (002) | Added by 006 |
| --- | --- | --- |
| solana | dex_volume_usd, active_trader_count, pump_launch_count, external_pool_count | swap_transaction_count, trade_leg_count |
| bsc | dex_volume_usd, active_trader_count, pancakeswap_pool_created_count, pancakeswap_lp_net_change_usd | swap_transaction_count, trade_leg_count |
| robinhood | dex_volume_usd, active_trader_count, uniswap_pool_created_count | swap_transaction_count, trade_leg_count |

All original per-chain metrics are retained. Only `swap_transaction_count` and
`trade_leg_count` are added to every chain. No metric is removed.

The TypeScript domain type (`src/domain/macro-daily.ts` L9-18) includes both
new metrics. The section union (L50) includes `'activity'`. The brief service
(L14-16, L41-42) maps them to section `'activity'`. Query definitions
(L67-78, L90-92) declare the corresponding Dune blueprints. All are consistent.

**Result: PASS**

## Check 4 — No external operations

Migration 006 contains only four `ALTER TABLE ... DROP/ADD CONSTRAINT`
statements. There is no:

- PostgreSQL migration execution (offline SQL definition only)
- Dune query execution or saved-query creation
- Feishu, Hermes, Lark, or CardKit operation
- External API call, provider invocation, or credential usage

The repair task spec (`MACRO-DAILY-TRADE-ACTIVITY-MIGRATION-REPAIR-014.json`
L11) explicitly confirms: "This repair is offline; it must not execute Dune,
database, Feishu, Hermes, or Lark operations before independent audit GREEN."

**Result: PASS**

## Check 5 — git status and untracked files

`git status --short` shows:

- 8 modified tracked files (SOL-PUMP-PROVENANCE, CURRENT_WAVE, ledger, SOL-DEV,
  SOL-HOLDER, SOL-PUMP, SOL-PUMP-PROVENANCE tasks, package.json) — none are in
  this task's write set.
- Many untracked files (`??`) including migration SQL files, audit reports,
  task specs, source, tests, docs, and fixtures.
- `006_macro_trade_activity_metrics.sql` is untracked (`??`), confirming it has
  not been committed to the repository yet.

`git diff --check` reports only LF/CRLF warnings on tracked modified files, no
whitespace errors.

**Result: PASS** — no unexpected tracked changes. All untracked files are
consistent with the project's ongoing development pattern.

## Acceptance commands

| Command | Result |
| --- | --- |
| `npm run harness:task -- validate ...AUDIT-014.json` | GREEN, 0 errors |
| `npm run typecheck` | Clean (no errors) |
| `npm test` | 60/60 pass, 0 fail |
| `npm run build` | Clean (no errors) |
| `git diff --check` | No whitespace errors (LF/CRLF warnings only) |

## Findings

No P0, P1, or P2 findings.

## Verdict

**GREEN** — Migration 006 correctly targets the actual PostgreSQL default
constraint name `macro_daily_chain_metrics_check`, preserves all existing
constraints (chain, section, coverage_status, completeness, Robinhood
partial_coverage, UNIQUE), adds only the `'activity'` section value and the
`swap_transaction_count` / `trade_leg_count` metrics to all three chains, and
contains no external operations. All acceptance commands pass.
