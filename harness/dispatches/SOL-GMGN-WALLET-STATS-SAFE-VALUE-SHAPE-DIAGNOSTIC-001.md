# Dispatch: SOL-GMGN-WALLET-STATS-SAFE-VALUE-SHAPE-DIAGNOSTIC-001

Run only as `implementer-sol-gmgn-wallet-stats-safe-value-shape-diagnostic-001`. Verify clean Git and both exact input hashes. Internally select one deterministic valid wallet without emitting it. Make one API-key-only 7d stats CLI invocation in disposable HOME/CWD. Inspect raw stdout only in memory, emit no values, and record only allowlisted metric alias names, recognized container locations, JSON types, and numeric-string lexical classes. Clear raw references after classification. No retry or second request.
